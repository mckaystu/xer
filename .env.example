# Neon Cloud — Xer graph storage
# Get your connection string: https://console.neon.tech → Project → Connect
#
# Use the POOLED connection string (required for Vercel serverless + local API):
#   postgresql://USER:PASSWORD@ep-XXXX-pooler.REGION.aws.neon.tech/neondb?sslmode=require
#
# Local: copy to xer/.env
# Vercel: Project → Settings → Environment Variables → NEON_DATABASE_URL

NEON_DATABASE_URL=

# Legacy alias (optional):
# DATABASE_URL=postgresql://...

# App password gate (HTTP Basic Auth). When set, the UI + API require login.
# Leave unset for open local development. Set both on Vercel for production.
# XER_APP_USERNAME=xer
# XER_APP_PASSWORD=

# Local dev only (not used on Vercel):
# XER_API_HOST=127.0.0.1
# XER_API_PORT=8765

# Optional — Domino DXL code auditor LLM enrichment
# OPENAI_API_KEY=
# XER_AUDIT_MODEL=gpt-4o-mini
# Minimum AI confidence (0-100) to act on FP / blind-spot / ownership verdicts (default 75)
# XER_AI_CONFIDENCE_MIN=75
